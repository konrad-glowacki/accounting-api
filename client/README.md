# Angular frontend for accounting app

Install bower packages (in the client path):
`bower install`

Install node packages (in the client path):
`npm install`

Run server (in the root path):
`node ./bin/client`
